test_variable = nil

#si no
if !test_variable #el signo de exclamación es para hacer la negación de una variable
    puts 'Se imprime'
end
#Se toma la variable test_variable, se invierte. Se toma nil, se invierte

#unless = si no
unless test_variable
    puts 'Se imprime'
end